apply plugin: 'groovy'
apply plugin: 'maven'

group = 'com.harshil.gradle'
version = '1.1.0'
archivesBaseName = 'felix'

repositories {
  mavenLocal()
  mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('build/repo'))
        }
    }
}
